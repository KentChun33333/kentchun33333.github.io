# Big picture

The model separates two relationship types that ordinary univariate forecasters collapse: temporal dependence inside a series and contemporaneous dependence across series. Known future signals stay visible while unknown future targets are masked. This makes forecasting resemble structured completion rather than iterative next-patch generation. [source-001]
